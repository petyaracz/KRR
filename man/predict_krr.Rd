% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/krr.R
\name{predict_krr}
\alias{predict_krr}
\title{Predict on new data using a trained KRR specification.}
\usage{
predict_krr(
  train_data,
  test_data,
  dist_df,
  word_col,
  outcome_col,
  sigma,
  alpha,
  link = c("identity", "logit"),
  epsilon = 0.001
)
}
\arguments{
\item{train_data}{Training data frame (words + outcome).}

\item{test_data}{Test data frame (words, and optionally outcome).}

\item{dist_df}{Long-format distance data frame. Must cover all
train–train and test–train word pairs.}

\item{word_col}{Name of the word/lemma column (string).}

\item{outcome_col}{Name of the outcome column (string).}

\item{sigma}{RBF kernel bandwidth.}

\item{alpha}{Ridge regularisation parameter.}

\item{link}{\code{"identity"} (default) or \code{"logit"}.}

\item{epsilon}{Clamping bound for logit link. Default 0.001.}
}
\value{
A tibble with \code{word}, \code{predicted} (original scale), and \code{observed}
(if \code{outcome_col} exists in \code{test_data}).
}
\description{
Predict on new data using a trained KRR specification.
}
\examples{
\dontrun{
preds <- predict_krr(train, test, dist,
                     word_col = "lemma", outcome_col = "p",
                     sigma = m$sigma, alpha = m$alpha, link = "logit")
}
}
